apiVersion: v1
kind: Service
metadata:
  name: {{ container_name }}
  namespace: {{ k8s_namespace }}

spec:
  selector:
    app: {{ container_name }}

  ports:
    - port: {{ container_port }}
      targetPort: {{ container_port }}

  type: ClusterIP