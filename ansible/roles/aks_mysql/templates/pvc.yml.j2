apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mysql-pvc
    namespace: {{ k8s_namespace }}
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: {{ pvc_storage }}

    storageClassName: {{ pvc_storage_class }}